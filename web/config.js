// Public config. Nothing secret goes here -- this file ships to every visitor.
//
// Leave API_BASE empty to run the page in demo mode (self-contained, no backend).
// Set it to your deployed Worker origin to go live, e.g.
//   API_BASE: "https://tibo-beg-api.your-subdomain.workers.dev"
window.TIBO_BEG_CONFIG = {
  API_BASE: "",
};
